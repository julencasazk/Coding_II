package program.DeustoCrash;

/**
 * Programa principal a ejecutar
 */
public class DeustoCrash {

    public static void main(String[] args) {
        
    }

    public Boolean buscaYQuitaLineas(TableroCaramelos tablero) {
        // TODO // Falta el sistema de mirar si hay rachas de 3 o más caramelos
        int streakF = 0; // Racha en la misma fila
        for (int i = 1; i < tablero.getGrid().length; i++) {
            // if caramelo[columa][fila].color == caramelo[columna-1][fila]
            //          racha++;

        }
        return true;
    }
}